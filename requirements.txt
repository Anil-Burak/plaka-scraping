# Project dependencies for scraping
requests==2.31.0
beautifulsoup4==4.12.2
lxml==4.9.3
pandas==2.2.2

# If you need Selenium, uncomment and pin a version:
# Selenium + webdriver helper + OCR used in `test.py`
selenium
webdriver-manager
ddddocr
